const LOCAL_STORAGE = {
  USER_AUTH: "userAuth",
  ACCESS_TOKEN: "acessToken",
};

const HTTP_STATUS = {
  SUCESS: ["10200"],
}

const SHOW_APPOVER = ["holiday"];

export { LOCAL_STORAGE, HTTP_STATUS, SHOW_APPOVER};
